<%# these variables were set in the render %>
<%= simple_form_for [chat, message], data: { controller: "reset-form" } do |f| %>
  <%= f.input :content, label: "", required: true, placeholder: "What is ..." %>
  <%= f.button :submit, 'Ask anything', class: 'btn btn-primary text-white' %>
<% end %>
